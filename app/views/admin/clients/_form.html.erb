<%= simple_form_for(@admin_client) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :adresse %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
